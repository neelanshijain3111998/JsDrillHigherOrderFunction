//Program=Filter Even no's from array
const items = [1, 2, 3, 4, 5, 5];

const id1 = require("../filter.js");
console.log(id1.PublicFilter(items, id1.PublicEvenNumbers)); //display even no from array

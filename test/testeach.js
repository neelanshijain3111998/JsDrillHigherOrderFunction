//The Each() method executes a provided function once for each array element.
//Program Multiply By Ten

const items = [1, 2, 3, 4, 5, 5];

const id1 = require("../each.js");
id1.PublicEach(items, id1.PublicMultiplyByTen);
